# SecureVault (Password Hasher + Vault)
Python + Tkinter + ttkbootstrap app for user registration, hashed passwords, and encrypted vaults.

## Quick start
1. `python -m venv venv`
2. Activate venv and `pip install -r requirements.txt`
3. `python main.py`


